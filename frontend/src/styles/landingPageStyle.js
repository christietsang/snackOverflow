import {style} from "typestyle";

export const landingWrapperStyle = style({
  height: "100vh"
})

export const btnWrapperStyle = style({
  marginTop: "70vh",
  display: "flex",
  width: "fit-content",
  margin: "auto",
  columnGap: "1rem"
})

export const btnStyle = style({
  padding: "1rem"
})